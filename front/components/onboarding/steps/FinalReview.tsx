import React from 'react';

interface FinalReviewProps {
  formData: {},
  onSubmit: () => void,
  onBack: () => void
}

const FinalReview = ({ formData, onSubmit, onBack }: FinalReviewProps) => {
  return (
    <div>

    </div>
  );
}

export default FinalReview;
