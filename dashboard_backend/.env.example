APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=dashboard_backend
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# LiveKit Voice Agent Configuration
# LIVEKIT_URL should point to the FastAPI endpoint for outbound calls
LIVEKIT_URL=http://localhost:8000/calls/outbound
LIVEKIT_API_KEY=your-api-key
LIVEKIT_SIP_TRUNK_ID=ST_your_trunk_id
LIVEKIT_WEBHOOK_URL=https://your-domain.com/api/v1/calls/webhook

# AI Service API Keys
OPENAI_API_KEY=your-openai-api-key
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
DEEPGRAM_API_KEY=your-deepgram-api-key
ELEVENLABS_API_KEY=your-elevenlabs-api-key
RIME_API_KEY=
STREAMELEMENTS_API_KEY=
SMALLEST_AI_API_KEY=

# OCR Configuration (for scanned/image-based PDFs)
# Set to true to enable OCR fallback for short extracted text
ENABLE_OCR=false

# OCR Service: Tesseract OCR (requires thiagoalessio/tesseract_ocr package)
# Note: Requires ImageMagick's 'convert' command and Tesseract binary
# Set TESSERACT_PATH to custom path if needed (default: tesseract)
# For better OCR quality, consider Google Cloud Vision, AWS Textract, or Azure Document Intelligence
TESSERACT_PATH=tesseract

# Qdrant Configuration (for Knowledge Base RAG)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=knowledge_base_chunks
QDRANT_VECTOR_SIZE=1536

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com

